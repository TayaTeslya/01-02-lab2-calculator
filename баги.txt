MC"-очистить память" M+"-прибавить в память" M-"-вычесть из памяти" MR"-что в памяти"
Что будет:
( ) 1/x MC M+ M- MR
x^2 x^3 x^y c / * <-
10^x x^(1/y)  7 8 9 -
e ln log 4 5 6 +
sin cos tan 1 2 3 =
ctg deg pi % 0 ,  =


ШО НАДА СДЕЛАТЬ:
+ Инпут:
	+ Вывод инпута
	+ Проверка при выводе
		+ Подряд:
			+ --
			+ ++
			+ **
			+ ..
			+ //
		+ Замена при двух знаках, кроме:
			+ *-
			+ /-
		+ Умножение между: 
			+ ( :
				+ число (
				+ pi (
				+ e (
			+ число :
				+ число pi
				+ число e
			+ корень:
				+ число √
				+ pi √
			+ тригонометрия (число, e, pi):
				+ sin
				+ cos
				+ tg
				+ ctg
				+ а также:
					+ ln
					+ lg

+ Стирать сразу:
	+ cos(
	+ sin(
	+ ctg(
	+ ln(
	+ lg(
	+ tg(
		c o s i n t g l


+ Память
+ Равно на 2 строки и сининьким цветом
+ функция для работа со строкой (сохраняем в переменную result):
	+ При нажатии . оставить 0 при надобности
	+ Ошибка:
		ЭТО ВСЕ ПЕРЕХВАТИТЬ КРЧ
		+ Несколько нажатий действий подряд (неск. +, неск. - и т.п.)
		+ Перед действием нет значения (степени, умножение, деление, корни, факториал)
		+ Деление на 0
		+ Корень минусового числа
	+ Замена:
		+ корень на Math.sqrt()
		+ все ^ на **
		+ e на Math.E
		+ Math.PI
		+ ln на Math.log()
		+ lg на Math.log10()
		+ деление и умножение на * /
		+ Math.sin() Math.cos() Math.tan() 1/Math.tan()
		+ % на /100	
	+ Радианы и градусы


+ по нажатию на с чистить error (если 0 в инпуте, еррор пустой)
+ Баги:
	+ lg и ln поменять местами 
	+ При градусах заменяется только первые тригонометрические ф-ции, остальные считаются в радианах
	+ replaceAll один заменить на replace (ctg tg)
	+ нельзя в инпуте писать, но вставлять можно
	+ 10^x будет считать как 100^x (замена 10^( на 100**( )
	+ Неправильный подсчет процентов (пример: 500 - 25% = 499,75 а должно быть 375)
	+ e√ не умножается как pi√ или число√

+ Фича:
	+ cos()^2+sin()^2 или наоборот, в строке ошибок вывести
	"Это не баг, это фича!"
	+ 9309706 (долбо*б) вывести "сам такой."

+ Можно считать за баг, но так НЕЛЬЗЯ ПИСАТЬ (НЕЛОГИЧНО):
	- ee
	- pipi
	- )число )pi )e 
	- Погрешность из-за js (тригонометрическое тождество равно не 1, а 1.000000002)
	